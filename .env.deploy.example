# Copy this file to .env.deploy and fill in your FTP credentials.
# .env.deploy is gitignored — never commit the real file.

FTP_HOST=85.9.216.223
FTP_USER=your_aapanel_user
FTP_PASS=your_password_here
FTP_REMOTE_DIR=/www/wwwroot/auto.ingmarvanrheenen.nl
